import React, { Fragment } from 'react';
import './Header.css';

const Header = () => {
  return (
    <Fragment>
      <h1 className="header-text">Pomidoro!</h1>
    </Fragment>
  );
};

export default Header;
